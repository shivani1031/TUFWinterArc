451. Sort Characters By Frequency:

Notes: in maxheap, max element is on the top 

class Solution {
public:
    string frequencySort(string s) {
        unordered_map<char, int>mp;
        priority_queue<int> maxh;
        for(char c:s) mp[c]++;

        for(int i =0;i<mp.size();i++)
        {
            maxh.push(mp[s[i]])
        }
    }
};
